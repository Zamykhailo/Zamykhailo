// Checks if the first variable is bigger than the second
const firstNumber = 3;
const secondNumber = 4;

if (firstNumber > secondNumber) {
  console.log("firstNumber is bigger than secondNumber");
} else {
  console.log("firstNumber is not bigger than secondNumber");
}